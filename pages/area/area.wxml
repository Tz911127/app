<view wx:if="{{condition}}">
  <picker-view indicator-style="height: 50px;" style="width: 100%; height:100vh;" value="{{value}}" bindchange="bindChange" class="citybody">
  <view class="cityheader">
      <view bindtap="close" class="city-cancel">取消</view>
      <view bindtap="open" class="city-true">确定</view>
    </view>
  <picker-view-column>
      <view wx:for="{{provinces}}" wx:key="item">{{item}}</view>
    </picker-view-column>
    <picker-view-column>
      <view wx:for="{{citys}}" wx:key="item">{{item}}</view>
    </picker-view-column>
 </picker-view>

  <!-- <view value="{{value}}" bindchange="bindChange">
    <view wx:for="{{provinces}}" wx:key="item" class="names" data-name='{{item}}' bindtap="onChangeShowState">
      {{item}}
      <view class="hide{{showView?'show':''}}">
      <view wx:for="{{citys}}" wx:key="item">
        <label class='cutab_two_name'>{{item}}</label>
      </view>
      </view>
      
    </view> -->


    <!-- <view class="hide{{showView?'show':''}}">
      <view class='cutab_two'>
        <label class='cutab_two_name'>{{item}}</label>
      </view>
    </view> -->


  <!-- </view> -->



</view>